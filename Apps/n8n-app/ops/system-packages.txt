# Host package list for the n8n VM (apt)
# Pin versions explicitly before provisioning the VM.

# Base utilities
ca-certificates
curl
gnupg
lsb-release

# Docker engine + compose (pin versions explicitly)
docker-ce=<PINNED_VERSION>
docker-ce-cli=<PINNED_VERSION>
containerd.io=<PINNED_VERSION>
docker-buildx-plugin=<PINNED_VERSION>
docker-compose-plugin=<PINNED_VERSION>
