# Cloudflared tunnel config (no secrets).
# Update the hostname once the tunnel and DNS are created.

ingress:
  - hostname: coolify.arshware.com
    service: http://localhost:8000
  - service: http_status:404
