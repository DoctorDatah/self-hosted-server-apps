# Alias Intent Hints

Use these hints only when explicit alias name is not present.

- Contains `n8n` and `setup` or `thingy`: `n8n-vm-setup`
- Contains `manual` and `setup`: `manual-vm-setup`
- Contains `install`, `all`, `app`, `vms`: `install-app-tier`
- Contains `restore`, `n8n`, `staging`: `restore-n8n-staging`

If ambiguous, ask one clarification question and do not execute.
