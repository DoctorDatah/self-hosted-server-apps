# Infisical Workflow

This folder contains the active Infisical workflow:

- `infisical.yml` — installs the Infisical CLI, uses `INFISICAL_TOKEN` + `INFISICAL_PROJECT_ID`,
  and exports Cloudflare secrets to:
  `VMs/Cloudflare Tunnel - via Docker/.infisical.cloudflare.env`

