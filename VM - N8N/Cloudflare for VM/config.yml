# SSH-only ingress for the VM.
# This tunnel is dedicated to SSH access (no app routing here).
# Use Cloudflare Access to protect the hostname.

ingress:
  - hostname: ssh_vm_n8n.arshware.com
    service: ssh://localhost:22

  - service: http_status:404
