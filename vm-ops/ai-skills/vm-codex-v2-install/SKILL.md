# vm-codex-v2-install

Standardizes VM install/bootstrap tasks via `repo_clone_or_update` and `vm_install` stages.

## Example

```bash
./runtime/vmcx run --alias n8n-vm-install --confirm --exec-mode local
```
