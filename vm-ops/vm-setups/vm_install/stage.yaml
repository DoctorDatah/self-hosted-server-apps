{
  "stage_id": "vm_install",
  "description": "Prepare VM baseline packages and runtime dependencies",
  "entrypoint": "stage.sh",
  "supports_exec_modes": ["local", "ssh"],
  "idempotent": true,
  "requires_confirmation": true,
  "allowed_target_types": ["vm", "host"],
  "required_params": [],
  "optional_params": [
    "install_utils",
    "utils_version_pin",
    "install_git",
    "git_version_pin",
    "install_python",
    "python_version_pin",
    "install_docker",
    "docker_version_pin",
    "create_network",
    "network_name",
    "install_infisical",
    "infisical_version_pin",
    "install_codex",
    "codex_version_pin"
  ],
  "policy_tags": ["install", "mutating"]
}
